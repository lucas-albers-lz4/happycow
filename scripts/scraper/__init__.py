"""Scraper package — Phase 5 of issue #30.

Modules: fetch (page acquisition + contamination guard), extract (schema +
DeepSeek + cache), merge (record building), cli (orchestration + entry).
Entry point: python scripts/scrape_happy_hours.py (backward-compat wrapper).
"""
